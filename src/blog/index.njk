---
layout: base.njk
title: Blog
pagination:
  data: collections.posts
  size: 10
  reverse: true
permalink: "/blog/{% if pagination.pageNumber > 0 %}page/{{ pagination.pageNumber + 1 }}/{% endif %}"
---

<h1>Blog</h1>
<p>A growing collection of tips, case studies, and updates from the engraving world.</p>
<p>We’ll share practical advice for running engraving shops, EPS preparation tricks, and news from T &amp; C Enterprises. (New posts coming soon — stay tuned.)</p>
<ul>
{% for post in pagination.items %}
  <li><a href="{{ post.url }}">{{ post.data.title }}</a> — <small>{{ post.date | date('yyyy-LL-dd') }}</small></li>
{% endfor %}
</ul>

{% if pagination.href.previous %}
  <a href="{{ pagination.href.previous }}">← Newer</a>
{% endif %}
{% if pagination.href.next %}
  <a style="float:right" href="{{ pagination.href.next }}">Older →</a>
{% endif %}
